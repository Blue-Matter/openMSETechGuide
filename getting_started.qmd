# Getting Started {#sec-openmse}

## Installing `openMSE`

The openMSE package is available on CRAN can be installed with:

```{r}
#| eval: false
install.packages('openMSE')
```

and loaded into the R session with:

library('openMSE')
