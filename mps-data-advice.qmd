# Management Procedures {#sec-mpsdataadvice}

```{r}
#| echo: false
#| include: false
library(openMSE)
```

This chapters in this part are incomplete.

@sec-objectdata describes the `Data` object. **This chapter is incomplete**.

@sec-obsdataconditioning describes how `Obs` objects are conditioned from observed data. **This chapter is incomplete**.

@sec-generatedata describess how simulated fishery data are generated in the model. **This chapter is incomplete**.

@sec-objectadvice describes the `Advice` object. **This chapter is incomplete**.

@sec-managementprocedures describes management procedures. **This chapter is incomplete**.

@sec-exampleMPs demonstrates some built-in example MPs. **This chapter is incomplete**.
